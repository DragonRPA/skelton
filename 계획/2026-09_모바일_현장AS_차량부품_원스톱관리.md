# 2026-09 AS팀원 전용 모바일 현장 AS 조치 및 차량 부품 실시간 자동 차감 원스톱 시스템 계획

- **일시**: 2026-09-02 22:45
- **프로젝트**: 기연리프트 ERP 시스템
- **카테고리**: 계획 (PLAN)

---

## 1. 목적 및 범위
- AS팀원(외근 정비사)의 현장 작업 환경(갤럭시 S24 393px 뷰포트, 장갑 착용, 한 손 조작)에 특화된 모바일 전용 UI/UX 구축.
- 현장 AS 완료 처리 시 사용 부품을 원터치 스텝퍼로 선택하면, 담당 기사 차량 재고(`mechanic_consumable_stocks`)가 실시간 자동 차감되는 원스톱 트랜잭션 구현.

## 2. 6대 모바일 특화 구현 요소
1. **50px 대형 스텝퍼 `[➖]` `[➕]` 부품 카트**: 키보드 가상 자판 팝업 차단, 터치 증감.
2. **엄지손가락 영역 슬라이드업 바텀시트**: 한 손 조작 최적화.
3. **빈출 조치 태그 1초 원터치 칩**: 타이핑 0% (방지봉 교체, 충전선 수리, 센서 리셋 등).
4. **원터치 전화걸기(`tel:`) & 내비게이션 딥링크**: 접수자 연락처 즉시 다이얼러 연동.
5. **다이렉트 후면 카메라 셔터**: `capture="environment"` 원터치 촬영 및 자동 압축.
6. **화면 최하단 고정 54px 초록색 완료 버튼**: 엄지손가락 존 단일 종결.

## 3. 실행 단계
- Step 1: 데이터 모델 및 `AppContext` 차량 부품 실시간 차감 트랜잭션 연동.
- Step 2: `FieldAsManagement.tsx` 모바일 전용 반응형 뷰 & 대형 터치 바텀시트 구현.
- Step 3: `tsc -b && vite build` 무결성 검증 및 프로덕션 배포.
