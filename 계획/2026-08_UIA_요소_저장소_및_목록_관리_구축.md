# 계획: UIA 요소 저장소(Repository) 및 목록 관리 시스템 구축

- **일시**: 2026-08-30 15:12
- **연결 발상**: [2026-08_UIA_기반_초고속_비전하이브리드_자동화.md](../발상/2026-08_UIA_기반_초고속_비전하이브리드_자동화.md)
- **연결 계획**: [2026-08_UIA_환경설정_기반_사전차단_및_토글_구축.md](../계획/2026-08_UIA_환경설정_기반_사전차단_및_토글_구축.md)
- **목표**: 캡처한 UIA 요소를 1회성 스텝에만 사용하는 한계를 넘어, `_Ref/uia_elements.json` 영구 저장소에 보관하여 목록으로 조회, 관리하고 스텝 설정 시 드롭다운으로 즉시 재사용할 수 있는 전사 관리 체계 구축.

---

## 1. 세부 실행 계획 (Action Plan)

1. **UIA 요소 저장소 코어 (`include/engine_uia.h`, `src/engine_uia.c`)**:
   - `UIARepoItem` 및 `UIARepository` 구조체 정의.
   - `_Ref/uia_elements.json` JSON 로드, 저장, 추가, 수정, 삭제 CRUD 엔진 구현.
   - `AppConfig_Init` 시 자동 로드 및 경로 관리.

2. **[📋 UIA 요소 목록] 전용 관리자 대화상자 (`UI_ShowUiaRepoManagerDialog`)**:
   - 리스트뷰 테이블: `#`, `별칭(Alias)`, `창 제목`, `요소 이름(Name)`, `자동화 ID`, `유형`, `등록일시`.
   - 하단 버튼: `[스텝으로 추가]`, `[동작 테스트]`, `[스파이로 새 요소 추가]`, `[삭제]`, `[닫기]`.
   - 행 더블클릭 시 즉시 스텝 생성 또는 속성 수정.

3. **UIA 스텝 설정 다이얼로그 연동 (`UI_ShowUiaStepDialog`)**:
   - 상단에 `[저장된 UIA 요소 선택]` 콤보박스 탑재.
   - 콤보박스 선택 변경 시 창 제목, 요소 이름, 자동화 ID 자동 완성.

4. **UIA 스파이 저장소 보관 연동 (`UI_ShowUiaSpyDialog`)**:
   - 스파이 화면에 `[이 요소를 저장소에 보관]` 버튼 추가.
   - 별칭 입력 후 `uia_elements.json`에 즉시 등록.

5. **메인 UI Row 3 툴바 연동 (`IDC_BTN_UIA_REPO`)**:
   - Row 3에 `[📋 UIA 요소 목록]` 버튼 배치 및 `WM_COMMAND` 핸들러 연동.
   - `UI_UpdateUiaControlState`로 UIA 활성화 상태에 따라 자동 활성/비활성.

6. **빌드 검증 및 릴리즈**:
   - `build.bat` 컴파일 (0 경고, 0 에러 검증).
