# 발상: Vercel 백엔드 PDF 대용량 병합 및 Storage 우회 파이프라인

- **일시**: 2026-08-17 19:35
- **프로젝트**: (주)기연리프트 전사 ERP (`Kiyuen_Lift`)
- **맥락**: 계약서/검수 서류 팩(14~15페이지) 및 다중 장비 출고 시 PDF 파일 취합·병합 연산의 프론트엔드/백엔드 역할 분담 논의

---

## 💡 핵심 발상 (Idea Concept)
브라우저(프론트엔드)가 구글 드라이브 파일 다운로드 및 PDF 병합을 직접 수행할 때 발생하는 사용자 PC 리소스 점유와 팝업 번거로움을 해결하기 위해, Vercel Serverless Function(백엔드 API) 및 Google Service Account를 활용하여 백그라운드에서 무음 자동 병합을 수행하는 파이프라인 구상.

---

## 🔍 제약사항 및 보류 배경 (Pending Decision)
- **Vercel Hobby(무료) 플랜의 4.5MB Payload 제약**:
  - 단일 응답 크기가 4.5MB로 제한되어 있어, 고해상도 스캔 서류나 2~3대 이상 다중 장비 묶음 시 파일 크기가 5~10MB로 증가하면 `413 Payload Too Large` 실패가 발생할 확률이 높음.
- **향후 해결책 구상 (Storage 경유 우회)**:
  - Vercel 백엔드가 결합된 대용량 PDF를 Supabase Storage에 직접 적재하고, 브라우저에는 1KB 크기의 다운로드 Signed URL만 반환하여 4.5MB 제한을 100% 우회하는 파이프라인으로 설계 발전 가능.
- **현재 방침**:
  - 당장은 브라우저 메모리(RAM) 기반 `pdf-lib` 결합 엔진(16GB RAM 환경에서 안정적)을 유지하며, 차후 대규모 배치 및 자동 발송 시스템 확장 시 백엔드 파이프라인으로 구현 검토.
