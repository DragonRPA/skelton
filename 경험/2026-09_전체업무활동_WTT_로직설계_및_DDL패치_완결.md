# 2026-09 전체 업무활동 WTT 로직 설계 및 DDL 패치 완결 경험

- **일시**: 2026-09-03 23:36
- **프로젝트**: 기연리프트 ERP 시스템
- **카테고리**: 경험 (EXPERIENCE)
- **연결 계획**:
  - [전사 연속 WTT 9대 파이프라인 실행계획](../계획/2026-09_전사_연속_WTT_9대_파이프라인_실행계획.md)
- **연결 경험**:
  - [현장 AS 접수 표준화 및 대차대조식 구축 경험](./2026-09_현장AS접수_표준화_및_대차대조식_구축.md)

---

## 1. 맥락 및 배경 (Context)

사장님의 명시적 지시사항("ㄹㅇ 후, 전체 업무활동 WTT 로직 설계하여 전체 순회 재수행. 필요한 DDL 패치를 직접 수행하여 완료")에 따라:
1. Supabase 원격 DB와 로컬 스키마(`schema.sql`) 간의 정합성을 전수 대사하여 미반영된 12개 테이블 및 5개 테이블 누락 컬럼을 색출.
2. `dev_exec_ddl` RPC를 통해 원격 DB DDL 패치를 직접 실행하고 PostgREST 스키마 캐시를 자동 갱신.
3. 무효 고스트 권한 85건(`userId IS NULL`)을 일괄 정돈.
4. 전사 16대 비즈니스 도메인을 아우르는 종합 Work-Through Test(WTT) 스위트를 설계하고 100% 무결 순회를 집행함.

---

## 2. 핵심 발견 및 결함 해소 (Key Discoveries & Solutions)

1. **원격 DB 12개 테이블 및 5개 테이블 누락 컬럼 DDL 전면 패치**:
   - 신규 생성 테이블: `annual_leave_quotas`, `leave_usages`, `overtime_records`, `payroll_closings`, `customer_bank_accounts`, `inbound_defect_details`, `asset_in_out_logs`, `repair_timeline_events`, `bank_account_initial_balances`, `settlement_payment_logs`, `prepaid_transactions`, `delinquency_action_logs` 12개 테이블 신규 생성 및 RLS 정책 100% 장착.
   - 컬럼 보완: `customers` (defaultPaidOptions, defaultProtection, defaultCheckedSpecs, specialNotes), `customer_sites` (paidOptions, protection, checkedSpecs), `contract_assets` (status, actualReturnDate), `billings` (invoiceId), `billing_invoices` (11개 필수 컬럼), `deliveries` (reconciliationStatus, confirmedCost, scheduledDate), `consumables` (name, spec, safetyStock).
   - 대사 결과: **Missing Tables: 0건, Missing Columns: 0건 100% 일치**.
2. **원격 고스트 권한 85건 정돈 완료 (헌장 5.3)**:
   - `userId IS NULL` 무효 권한 85건 원격 정리 완료 ➔ 정상 계정 132건 100% 보존.
3. **전체 업무활동 WTT 16대 파이프라인 종합 스위트 설계 (`wtt_full_business_activity_suite.cjs`)**:
   - 조직/권한, 자산/모델, 고객/현장, 계약체결, 배차출고, 대차교체, 계약변경, 현장AS, 반납입고, 정비수불, 소모품구매, 매출청구, 수납대사, 채권연체, 매입정산, 회계결산 등 16대 도메인 31개 항목 전수 검증.
   - **검증 결과: 31개 항목 100.0% 통과 (0 error)**.

---

## 3. 배포 및 검증 결과

- **TypeScript & Vite Build**: `tsc -b && vite build` ➔ 1.27s (0 error) 통과
- **버전 넘버**: `v1.3.0.Build.109` (2026-09-03 23:36)
- **Git Commit & Push**: `f0ef7f7` (`main` 브랜치)
- **Vercel 프로덕션 배포**: `https://kiyuen-lift-ka5g72hn8-dragonrpa.vercel.app` (● Ready)
- **Vercel 슬롯 청정 유지 (헌장 6.3)**: 초과 슬롯(`7zei1em5g`) 자동 Purge 완료 ➔ 상시 최대 12개 슬롯 유지 완료.
