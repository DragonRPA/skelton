# 🔬 [경험] Neon Serverless PostgreSQL 실시간 연동 및 RPA 스키마 구축

- **작성일시**: 2026-08-21 15:38 (KST)
- **프로젝트**: UBUS_contract & Universal_RPA_Recorder (RPA Script Editor)
- **버전**: v1.2.0.Build.4

---

## 1. 맥락 및 배경
- Supabase 무료 프로젝트 2개 한도 소진에 따라 대체 클라우드 DB로 **Neon Serverless PostgreSQL** 채택
- 비용 0원 유지(Scale-to-zero) 및 최대 100개 프로젝트 지원 혜택 활용

## 2. 핵심 수행 내용
1. **싱가포르 리전 연결 검증**: 지연시간 ~700ms로 초고속 연결 확인
2. **2대 핵심 RPA 테이블 스키마 자동 구축**:
   - `rpa_contract_logs`: 계약번호, 파일명, 처리상태, OCR JSON 데이터, 소요시간
   - `rpa_batch_runs`: 배치 ID, 총 파일수, 성공수, 실패수, 완료시각
3. **스니펫 라이브러리 연동**: `🗄️ 데이터베이스 (Neon PostgreSQL)` 카테고리 추가로 에디터에서 원클릭 SQL 삽입 지원

## 4. [확장] 단일 테이블(rpa_scripts) 기반 모듈형 스크립트 저장소 구축 (v1.3.0.Build.5)
- **추가 일시**: 2026-08-21 15:46 (KST)
- **주요 성과**:
  - `rpa_scripts` 단일 테이블 DDL 가동 (`name`, `title`, `category`, `code` 등 8개 컬럼)
  - 조인(JOIN) 제로의 1줄 쿼리 구조로 복잡도 0 달성
  - GUI 에디터 상단에 `[☁️ DB 모듈 라이브러리]` 관리 모달 탑재로 조회, 에디터 삽입, 즉시 DB 등록/수정/삭제 원스톱 연동
