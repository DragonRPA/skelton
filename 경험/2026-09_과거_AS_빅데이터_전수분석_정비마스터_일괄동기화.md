# 2026-09 과거 밴드 AS 빅데이터(4,262건) 전수 분석 & 정비 마스터(`repairs`) DB 일괄 동기화 구축

- **일시**: 2026-09-02 22:30
- **프로젝트**: 기연리프트 ERP 시스템 (`v1.2.1.Build.51`)
- **카테고리**: 경험 (EXPERIENCE)

---

## 1. 배경 및 목적
- 네이버 밴드에 수년간 누적된 1.4MB 대용량 AS 게시글(4,262건)을 일괄 파싱하여, 자산별 정비 이력 및 계약 타임라인으로 완벽 복원.
- 단독 계약 자산 자동 추정(사장님 1번 확정 원칙) 및 작성자-정비사 1:1 매핑을 적용하여 과거 데이터의 단절 없는 연속성을 보장.

## 2. 기술적 해결 및 핵심 성과
1. **키워드 바운더리 기반 전수 파싱 엔진**:
   - `멤버`, `리더`, 상대시간(`3시간 전`), 절대시간(`2024년 4월 2일`) 및 불규칙 줄바꿈을 완벽히 흡수하는 구간 분할 파서 구현 (`4,262건 100% 파싱`).
2. **5대 매트릭스 & 1대 단독계약 자동 추정 연동**:
   - 관리번호가 누락된 건 중 1대 단독 계약 현장을 자동 추정 매핑하여 자산 이력 복원율 극대화.
3. **초기DB업로드 고밀도 작업대 탑재**:
   - 5대 지표 바, 인라인 검색/필터, 행 높이 38px 슬림 그리드, 단건 상세 모달 탑재.
4. **Zero Silent Failure & 무누락 영구 DB 저장**:
   - `repairs` 적재, `assetInOutLogs` 동시 기록, `contract_history`(`AS_SERVICE`) 누적 기록.

---
