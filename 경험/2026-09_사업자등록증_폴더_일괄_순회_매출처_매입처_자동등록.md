# [경험] 사업자등록증 폴더 일괄 순회 Vision AI 분석 및 매출처/매입처 자동 등록 구현

- **일자**: 2026-09-12
- **구현 결과**:
  1. `src/services/batchBusinessLicenseService.ts`:
     - 폴더 선택 및 Drag & Drop recursive reader 구현 (`webkitGetAsEntry`, `createReader`, 지원 확장자 필터링).
     - Vision AI 단건 처리기 (`processSingleLicense`): Groq LPU Vision / Gemini Flash 연동, 429 지수 백오프, 증빙 영구 스토리지 업로드(`customer_licenses`, `vendor_licenses`).
     - Customer/Vendor DB 중복 대조 로직: 사업자번호 10자리 클린 정규화 대조 ➔ 법인/주식회사 접두어 제거 정규화 상호명 2차 대조 ➔ 누락/미상 필드만 선택적 보완(`SUCCESS_UPDATED`) 또는 신규 생성(`SUCCESS_NEW`).
     - 순차 큐 배치 실행기 (`runBatchBusinessLicenses`): 건당 850ms 안정 지연 및 AbortController 일시정지 지원.
  2. `src/components/BatchBusinessLicenseModal.tsx`:
     - 전사 표준 헌장 3.1 무수식어 건조한 명사·동사 UI 및 3.5 좌상단 ➔ 우하단 Gutenberg Z-패턴 적용.
     - 진행률 HUD (전체, 처리, 대기, 신규, 보완, 실패 카운터 배지) 및 행 높이 38px 고밀도 실시간 스트리밍 대사 그리드.
     - 감사 대사용 결과 엑셀 다운로드(`exportToExcel`) 연동.
  3. `Customers.tsx`, `Vendors.tsx`, `MobileCustomerManage.tsx` 3대 화면에 원클릭 모달 트리거 버튼 배치 및 권한(`canSave`) 제어.
- **학습 내용**:
  - 대량 배치 AI 처리에서는 병렬 `Promise.all`이 아닌 순차 지연 큐와 429 지수 백오프가 필수적이며, 사용자에게 실시간 스트리밍 피드와 엑셀 감사로그를 제공할 때 시스템 신뢰도가 비약적으로 향상됨.
